name: .NET

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '6.0.x'

    - name: Install dependencies
      run: dotnet restore "Generador de oferta.sln"

    - name: Build
      run: dotnet build "Generador de oferta.sln" --configuration Release --no-restore

    - name: Test
      run: dotnet test "Generador de oferta.sln" --no-build --verbosity normal

    - name: Publish
      run: dotnet publish "Generador de oferta.csproj" --configuration Release --output ./publish

    - name: List publish directory
      run: ls -al ./publish

    - name: Deploy to GitHub Pages
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./publish


